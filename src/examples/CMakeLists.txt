add_subdirectory(blinky)
if(CONFIG_EXAMPLE_PIC32MZ)
  add_subdirectory(pic32mz)
endif()
if(CONFIG_EXAMPLE_MINGW)
  add_subdirectory(win32)
endif()
if(CONFIG_EXAMPLE_POSIX)
  add_subdirectory(posix)
endif()
